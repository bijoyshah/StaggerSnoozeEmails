# StaggerSnoozeEmails
A Chrome extension that lets you snooze multiple Gmail emails in staggered time increments. Select emails in priority order, set a start time and increment, and let the extension do the rest.
